<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.PlatoCalendar" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/dark</item>
    </style>

    <style name="Theme.PlatoCalendar.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/dark</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_plato_calendar_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.PlatoCalendar</item>
    </style>

</resources>
